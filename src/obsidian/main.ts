import { Plugin } from "obsidian";

/**
 * Platzhalter des Fundaments. Aufgabe 2 ersetzt ihn temporaer durch den Spike, Aufgabe 10
 * durch die echte Plugin-Verdrahtung.
 *
 * Ohne Rumpf und ohne `console.log`: das im Plan vorgeschlagene Log verletzt
 * `obsidianmd/no-console` — dieselbe Regel, die der Store-Scanner faehrt.
 */
export default class PaperlessStoragePlugin extends Plugin {
  onload(): void {
    // Noch nichts zu laden.
  }
}
