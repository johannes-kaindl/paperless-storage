# Welcome

This is a generic fixture vault for `paperless-storage` — used by the GUI smoke driver (`scripts/gui-smoke.ts`) and by the README screenshot recipe (`scripts/shots.ts`, skill `readme-shots`). Content here is deliberately generic and English: whatever ends up in a screenshot ships with the repo.

The smoke driver creates and removes its own scratch note and `.paperless` stub at runtime.
